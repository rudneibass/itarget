<section class="section-actions">
    <div class="section-actions-btn-parent">
        <button
            class="btn btn-primary btn-float section-actions-btn"
            onclick="admin.addTab({tabId:'form-jogo', title: 'Novo Jogo', url: '/admin/jogo/form'})"
        >
            <i class="fa fa-plus"></i>
            <span class="btn-label">Novo</span>
        </button>
    </div>
    <div class="section-actions-btn-parent">
        <button class="btn btn-warning btn-float section-actions-btn" id="btn-toggle-barra-jogo">
            <i class="fa fa-search"></i>
            <span class="btn-label">Pesquisar</span>
        </button>
    </div>
</section>

<section class="section-search-bar hide" id="section-search-bar-jogo">
    <div class="row search-bar" id="search-bar-jogo">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header bg-primary-light"><span class="text-muted">Pesquisa por...</span></div>
                <div class="card-body">
                    <form id="formSearchJogo">
                        <div class="row">
                            <div class="col-md-4">
                                <div class="input-group mb-3">
                                    <input type="text" name="uuid" id="uuid" class="form-control" placeholder="Código">
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="input-group mb-3">
                                    <input type="text" name="nome" id="nome" class="form-control" placeholder="Nome">
                                </div>
                            </div>
                            <div class="col-md-4">
                                <div class="input-group mb-3">
                                    <select name="ativo" id="ativo" class="form-select">
                                        <option value="">Status</option>
                                        <option value="true">Ativo</option>
                                        <option value="false">Inativo</option>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
                <div class="modal-footer p-2 border-top-0">
                    <button type="button" class="btn btn-dark btn-sm" id="btn-pesquisar-jogo">
                        <i class="fa fa-search"></i> Pesquisar
                    </button>
                </div>
            </div>
        </div>
    </div>
</section>

<section>
    <div class="page-info-bar shadows-default">
        <small><i class="fa fa-gamepad"></i></small>
        <small>Jogos</small>
    </div>
</section>

<section>
    <div class="list-table-container">
        <table class="table table-sm table-striped table-hover width-100">
            <thead id="thead-jogo"></thead>
            <tbody id="tbody-jogo"></tbody>
        </table>
        <div class="text-center">
            <span class="pt-5" id="messages-jogo"></span>
        </div>
    </div>
    <div class="pagination-bar-default">
        <div>
            <small class="gray-default" id="total-jogo"></small>
        </div>
        <div>
            <i class="fa fa-chevron-left gray-default cursor-pointer" id="anterior-jogo"></i>
            &emsp;
            <span class="gray-default" id="numeracao-jogo"></span>
            &emsp;
            <i class="fa fa-chevron-right gray-default cursor-pointer" id="proximo-jogo"></i>
        </div>
    </div>
</section>

<script>
    (() => {
    const jogoList = {
        formSearchId: 'formSearchJogo',
        list: ({ url, formSearchId }) => {
            $.ajax({
                url,
                cache: false,
                type: 'GET',
                data: $('#' + formSearchId).serialize(),
                dataType: 'json',
                beforeSend: function () {
                    $('#messages-jogo').empty();
                    $('#tbody-jogo').empty();
                    admin.loading(true);
                },
                error: function () {
                    admin.loading(false);
                    $('#tbody-jogo').empty();
                    $('#messages-jogo').empty();
                    $('#messages-jogo').html("<h6 class='text-muted text-center'>Não foi possivel executar a ação desejada, favor entrar em contato com o administrador.</h6>");
                },
                success: function (response) {
                    var source = response?.data || response || [];
                    var filters = $('#' + formSearchId).serializeArray().reduce((obj, item) => {
                        obj[item.name] = item.value;
                        return obj;
                    }, {});

                    var data = source.filter((item) => {
                        const byUuid = !filters.uuid || String(item.uuid || '').toLowerCase().includes(String(filters.uuid).toLowerCase());
                        const byNome = !filters.nome || String(item.nome || '').toLowerCase().includes(String(filters.nome).toLowerCase());
                        const byAtivo = !filters.ativo || String(!!item.ativo) === String(filters.ativo);
                        return byUuid && byNome && byAtivo;
                    });

                    if (!data[0]) {
                        admin.loading(false);
                        $('#tbody-jogo').empty();
                        $('#messages-jogo').empty();
                        $('#messages-jogo').html("<br/><br/><h6 class='text-muted text-center'>Consulta não obteve resultados.</h6>");
                        return;
                    }

                    admin.loading(false);
                    $('#tbody-jogo').empty();
                    $('#messages-jogo').empty();

                    var tamanhoPagina = 10;
                    var pagina = 0;

                    function paginar() {
                        for (var i = pagina * tamanhoPagina; i < data.length && i < (pagina + 1) * tamanhoPagina; i++) {
                            $('#tbody-jogo').append(
                                `<tr>
                                    <td class="p-3" style="width:90%; vertical-align: middle;">
                                        <div>
                                            <small>
                                                <b>Nome:</b> ${data[i].nome || ''}
                                                ${data[i].ativo
                                                    ? '&emsp;<span class="badge bg-primary">Ativo</span>'
                                                    : '&emsp;<span class="badge bg-secondary">Inativo</span>'
                                                }
                                            </small>
                                            <br/>
                                            <small><b>Descrição:</b> ${data[i].descricao || '-'}</small>
                                            <br/>
                                            <small><b>URL:</b> ${data[i].url || '-'}</small>
                                            <br/>
                                            <small><b>Custo em moedas:</b> ${data[i].custoMoedas ?? 0}</small>
                                            <br/>
                                            <small><b>Código:</b> ${data[i].uuid || ''}</small>
                                        </div>
                                    </td>
                                    <td style="width: 8%; vertical-align: middle;">
                                        <center>
                                            <button
                                                class="btn btn-dark btn-sm w-100 mb-2"
                                                onclick="admin.addTab({tabId:'form-jogo', title: 'Editar Jogo', url: '/admin/jogo/form/uuid/${data[i].uuid}'})"
                                            >
                                                <i class="fa fa-pencil"></i> Editar
                                            </button>
                                            ${data[i].ativo
                                                ? `<button class="btn btn-dark btn-sm w-100 mb-2" onclick="jogoList.toggleAtivo('${data[i].uuid}', false)">
                                                    <i class="fa fa-download"></i> Desativar
                                                </button>`
                                                : `<button class="btn btn-dark btn-sm w-100 mb-2" onclick="jogoList.toggleAtivo('${data[i].uuid}', true)">
                                                    <i class="fa fa-upload"></i> Ativar
                                                </button>`
                                            }
                                            <button class="btn btn-dark btn-sm w-100 mb-2" onclick="jogoList.remove('${data[i].uuid}')">
                                                <i class="fa fa-trash-o"></i>
                                                <span> Excluir</span>
                                            </button>
                                        </center>
                                    </td>
                                </tr>`
                            );
                        }
                        $('#numeracao-jogo').text('Página ' + (pagina + 1) + ' de ' + Math.ceil(data.length / tamanhoPagina));
                        $('#total-jogo').text('1 à ' + tamanhoPagina + ' registros por página ');
                    }

                    function ajustarBotoes() {
                        $('#proximo-jogo').prop('disabled', data.length <= tamanhoPagina || pagina >= Math.ceil(data.length / tamanhoPagina) - 1);
                        $('#anterior-jogo').prop('disabled', data.length <= tamanhoPagina || pagina === 0);
                    }

                    $(function () {
                        $('#proximo-jogo').off('click').on('click', function () {
                            if (pagina < data.length / tamanhoPagina - 1) {
                                $('#tbody-jogo').empty();
                                pagina++;
                                paginar();
                                ajustarBotoes();
                            }
                        });
                        $('#anterior-jogo').off('click').on('click', function () {
                            if (pagina >= 1) {
                                $('#tbody-jogo').empty();
                                pagina--;
                                paginar();
                                ajustarBotoes();
                            }
                        });
                        paginar();
                        ajustarBotoes();
                    });
                },
            });
        },
        toggleAtivo: (uuid, ativo) => {
            $.ajax({
                url: `${admin.baseUrl}/jogo/${uuid}`,
                type: 'PATCH',
                contentType: 'application/json',
                data: JSON.stringify({ ativo }),
                beforeSend: function () {
                    admin.loading(true);
                },
                success: function () {
                    admin.loading(false);
                    toastr['success']('Status atualizado com sucesso.');
                    jogoList.list({
                        url: admin.baseUrl + endpoints.jogo.all,
                        formSearchId: jogoList.formSearchId,
                    });
                },
                error: function () {
                    admin.loading(false);
                    toastr['error']('Não foi possível atualizar o status.');
                },
            });
        },
        remove: (uuid) => {
            bootbox.confirm({
                title: 'Atenção',
                message: 'Tem certeza que deseja remover este registro?',
                buttons: {
                    confirm: {
                        label: '<i class="fa fa-check"></i> Confirmar',
                        className: 'btn-sm btn-primary',
                    },
                    cancel: {
                        label: '<i class="fa fa-undo"></i> Cancelar',
                        className: 'btn-sm btn-dark',
                    },
                },
                callback: function (result) {
                    if (!result) {
                        return;
                    }

                    $.ajax({
                        url: `${admin.baseUrl}/jogo/${uuid}`,
                        type: 'DELETE',
                        beforeSend: function () {
                            admin.loading(true);
                        },
                        success: function () {
                            admin.loading(false);
                            toastr['success']('Jogo removido com sucesso.');
                            jogoList.list({
                                url: admin.baseUrl + endpoints.jogo.all,
                                formSearchId: jogoList.formSearchId,
                            });
                        },
                        error: function () {
                            admin.loading(false);
                            toastr['error']('Não foi possível remover o jogo.');
                        },
                    });
                },
            });
        },
    };

    window.jogoList = jogoList;

    document.getElementById('btn-toggle-barra-jogo').addEventListener('click', () => {
        const barra = document.getElementById('section-search-bar-jogo');
        barra.classList.toggle('visible');
    });

    document.getElementById('btn-pesquisar-jogo').addEventListener('click', () => {
        const barra = document.getElementById('section-search-bar-jogo');
        barra.classList.toggle('visible');
        jogoList.list({
            url: admin.baseUrl + endpoints.jogo.all,
            formSearchId: jogoList.formSearchId,
        });
    });

    jogoList.list({
        url: admin.baseUrl + endpoints.jogo.all,
        formSearchId: jogoList.formSearchId,
    });
    })();
</script>
