import { endpoints } from "@utils/endpoints";
import { isObject } from "./isObject";
import jsonToHtmlList from "./jsonToHtmlList";

export const utils = {
    isObject,
    jsonToHtmlList,
    endpoints
}